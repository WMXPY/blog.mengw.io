/**
 * @param {number} x
 * @param {number} y
 * @return {number}
 */
var hammingDistance = function (x, y) {
    x = parseInt(x.toString(2));
    y = parseInt(y.toString(2));
    // console.log(x + 1);

};
hammingDistance(2, 4);